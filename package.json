{
  "name": "neepoo-dev-tools",
  "private": true,
  "type": "module",
  "description": "本地资产流水线：处理减脂进度照并写入文章表格",
  "scripts": {
    "photo": "node --env-file-if-exists=.env scripts/photo.mjs",
    "photo:dry": "node --env-file-if-exists=.env scripts/photo.mjs --dry-run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "sharp": "^0.33.5"
  }
}
